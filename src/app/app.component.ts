import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  templateUrl: `<router-outlet></router-outlet>`, // placeholder for pages
})
export class AppComponent {
  title = 'smartTaskManager';
}
